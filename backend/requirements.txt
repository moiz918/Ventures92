# Web framework & ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # [standard] pulls in watchfiles for --reload

# Database
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Settings & environment
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# Authentication
# NOTE: passlib 1.7.4 reads bcrypt.__about__.__version__ which was removed in
# bcrypt 4.1+. Pin to 4.0.1 — the same version Anthropic uses internally — to
# avoid the runtime ValueError that surfaces inside the FastAPI login handler.
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-jose[cryptography]>=3.3.0
email-validator>=2.1.0
