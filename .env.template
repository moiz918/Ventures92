# =============================================================================
# Ventures 92 — Environment Template
# Copy this file to .env and fill in your values.
# NEVER commit the real .env file.
# =============================================================================

# PostgreSQL
POSTGRES_USER=ventures_user
POSTGRES_PASSWORD=pass
POSTGRES_DB=ventures92

# pgAdmin
PGADMIN_EMAIL=admin@ventures92.com
PGADMIN_PASSWORD=pass

# =============================================================================
# Authentication
# =============================================================================
# Generate a long random secret in production:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
# At minimum 64 characters of entropy. NEVER reuse this between environments.
JWT_SECRET=change_me_locally_to_a_64_char_url_safe_random_string_xxxxxxxxxxxxxxxxxxxxxxx
